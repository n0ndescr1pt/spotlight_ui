import 'package:flutter/material.dart';

class ArrowSettings {
  final Color color;
  final Size size;

  const ArrowSettings({
    this.color = Colors.white,
    this.size = const Size(24, 12),
  });
}